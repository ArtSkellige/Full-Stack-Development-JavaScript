** start of script.js **

function myReplace(str, before, after) {
  const isUpperCase = before.charAt(0) === before.charAt(0).toUpperCase();

  let newWord;
  if (isUpperCase) {

    newWord = after.charAt(0).toUpperCase() + after.slice(1);
  } else {

    newWord = after.charAt(0).toLowerCase() + after.slice(1);
  }

  return str.replace(before, newWord);
}

** end of script.js **

